import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BasicAnimationScreen = () => {
  return (
    <View>
      <Text>BasicAnimationScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BasicAnimationScreen;
